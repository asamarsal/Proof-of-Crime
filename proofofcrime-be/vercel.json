{
    "version": 2,
    "installCommand": "npm install",
    "buildCommand": "npm run db:generate",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/index"
        }
    ]
}